<Window x:Class="ScadaGUI.AddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dodaj tag / alarm" Height="500" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Tip:" VerticalAlignment="Center" Width="80"/>
            <ComboBox x:Name="cmbType" Width="200" SelectionChanged="CmbType_SelectionChanged">
                <ComboBoxItem Content="AI" IsSelected="True"/>
                <ComboBoxItem Content="AO"/>
                <ComboBoxItem Content="DI"/>
                <ComboBoxItem Content="DO"/>
                <ComboBoxItem Content="Alarm"/>
            </ComboBox>
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <StackPanel x:Name="pnlFields"/>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Dodaj" Width="90" Height="30" Margin="0,0,10,0" Click="BtnAdd_Click"/>
            <Button Content="Otkaži" Width="90" Height="30" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
